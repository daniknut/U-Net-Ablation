This contains the images and annotations used for the annotation consistency experiment in the CVPR'17 paper. 64 images are annotated again by our expert annotator. The original annotation is in the folder original_ade20k, while repeated annotation is in the folder repeat_ade20k. 20 images are further annotated by two external annotators, which are inside the folders repeat_participant1 and repeat_participant2.